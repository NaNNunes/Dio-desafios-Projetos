// Fiuncoes imediatamentes invocadas -- podem ser anonimas --- usadas para codigo principal
(function(){
    const altura = 1.85
    const peso = 68
    let imc = peso / (altura * altura)
    console.log(imc.toFixed(2))
})();