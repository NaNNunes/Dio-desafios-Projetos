// Funções temporárias --- podem ser anonimas --- usadas para codigos principal
(function () {
    const peso = 68
    const altura = 1.85
    let imc = peso / (altura * altura)
})();
    // ao ser lida é invocada/exibida imediatamente